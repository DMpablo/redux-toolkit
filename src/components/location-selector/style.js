import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        width: '80%',
        marginBottom:10
      },
      preview:{
        width: '100%',
        height: 200,
        marginBottom: 10,
        justifyContent: "center",
        alignItems: "center",
        borderColor: "purple",
        borderWidth:1
      },
      image:{
        width: '100%',
        height: '100%'
      }
});